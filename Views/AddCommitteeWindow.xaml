<Window x:Class="Foscamun2026.AddCommitteeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Committee"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Committee Name:"/>
        <TextBox x:Name="NameBox" Grid.Row="1" Margin="0 5"/>

        <TextBlock Text="Topic A:" Grid.Row="2" Margin="0 10 0 0"/>
        <TextBox x:Name="TopicABox" Grid.Row="3" Margin="0 5"/>

        <TextBlock Text="Topic B:" Grid.Row="4" Margin="0 10 0 0"/>
        <TextBox x:Name="TopicBBox" Grid.Row="5" Margin="0 5"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 20 0 0">
            <Button Content="Cancel" Width="80" Margin="0 0 10 0" Click="Cancel_Click"/>
            <Button Content="Add" Width="80" Click="Add_Click"/>
        </StackPanel>
    </Grid>
</Window>
