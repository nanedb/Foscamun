<Window x:Class="Foscamun2026.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Setup" Height="400" Width="400"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>      <!-- Titolo + ComboBox -->
            <RowDefinition Height="*"/>         <!-- Lista -->
            <RowDefinition Height="Auto"/>      <!-- Pulsanti -->
        </Grid.RowDefinitions>


        <!-- Titolo + ComboBox -->
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="{DynamicResource SelectLanguageTitle}" FontSize="18"/>
            <ComboBox x:Name="LanguageSelector" SelectionChanged="LanguageSelector_SelectionChanged">
                <ComboBoxItem Tag="en-US">English</ComboBoxItem>
                <ComboBoxItem Tag="fr-FR">Français</ComboBoxItem>
                <ComboBoxItem Tag="es-ES">Español</ComboBoxItem>
            </ComboBox>
        </StackPanel>

        <!-- Lista commissioni -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Committees:" FontSize="18" Margin="0,20,0,10"/>
            <ListBox x:Name="CommitteeList" Height="150"/>
        </StackPanel>

        <!-- Pulsanti in basso -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center"
                    Margin="0,20,0,0">
            <Button Content="{DynamicResource ExitButton}"
                    Width="100" Margin="10,0,10,0"
                    Click="ExitButton_Click"/>
        </StackPanel>

    </Grid>
</Window>
