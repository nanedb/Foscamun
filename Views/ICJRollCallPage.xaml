<Page x:Class="Foscamun2026.Views.ICJRollCallPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
      mc:Ignorable="d"
      d:DesignWidth="800"
      d:DesignHeight="450"
      Background="{DynamicResource PrimaryBrush}"
      Foreground="{DynamicResource TextPrimaryBrush}"
      Title="ICJ Roll Call">

    <Grid Margin="20 15 20 15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

         <!-- Titolo -->
        <TextBlock Grid.Row="0"
                   Text="Roll Call" 
                   Style="{StaticResource H3}"
                   Margin="0,0,0,10"/>

		<!-- Contenuto -->
		<Grid Grid.Row="1">
			<Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

            <!-- Logo e Board -->
            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Logo -->
                <Viewbox Grid.Column="0" Grid.Row="0" Grid.RowSpan="3"
                         Margin="0,0,0,10" HorizontalAlignment="Left" VerticalAlignment="Top" MaxHeight="150" MinHeight="100">
				    <svgc:SvgViewbox Source="{Binding CommitteeLogoPath}"
                                     Stretch="Uniform"/>
			    </Viewbox>
                
                <!-- Board -->
                <TextBlock Grid.Column="1" Grid.Row="0"
                            Text="President"
                            HorizontalAlignment="Left"/>
                <TextBlock Grid.Column="1" Grid.Row="1" 
                            Text="Vice President"/>
                <TextBlock Grid.Column="1" Grid.Row="2" 
                            Text="Moderator"
                            Margin="0,0,0,5"/>
                
                <TextBlock Grid.Column="2" Grid.Row="0"
                            Text="President"/>
                <TextBlock Grid.Column="2" Grid.Row="1" 
                            Text="Vice President"/>
                <TextBlock Grid.Column="2" Grid.Row="2" 
                            Text="Moderator"
                            Margin="0,0,0,5"/>
            </Grid>
			
			<!-- Contenuto placeholder -->
			<Border Grid.Row="1"
					Background="{DynamicResource SurfaceBrush}"
					CornerRadius="{StaticResource CornerRadiusMedium}">
				<TextBlock Text="{DynamicResource ICJRollCallPlaceholder}"
						   Style="{StaticResource Body}"
						   HorizontalAlignment="Center"
						   VerticalAlignment="Center"
						   TextAlignment="Center"/>
			</Border>
		</Grid>
    </Grid>
</Page>